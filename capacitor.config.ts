import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'com.klameet.app', appName: 'KLA MEET', webDir: 'out', server: { androidScheme: 'https' } };
export default config;