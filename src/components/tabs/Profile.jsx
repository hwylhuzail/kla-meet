export default function Profile({user,isAdmin,adminEmails,onShowPrivacy,onShowTerms}){
  return <div className="max-w-md mx-auto p-4"><h2 className="font-black text-white">Profile — {user?.email}</h2><p className="text-[10px] mt-2 text-white/60">{isAdmin?'ADMIN - full access':'Normal user'} • {adminEmails.includes(user?.email?.toLowerCase())?'Verified admin':'Not admin'}</p><div className="flex gap-2 mt-4"><button onClick={onShowPrivacy} className="bg-zinc-800 px-3 py-1 rounded-full text-[10px] text-white">Privacy</button><button onClick={onShowTerms} className="bg-zinc-800 px-3 py-1 rounded-full text-[10px] text-white">Terms</button></div><button onClick={async()=>{ localStorage.clear(); location.reload() }} className="mt-6 bg-red-600 px-4 py-2 rounded-full text-xs w-full text-white">Logout + Clear</button></div>
}